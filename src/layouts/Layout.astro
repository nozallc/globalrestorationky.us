---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE } from '../lib/site';
import { generateOrganizationSchema } from '../lib/schema';
import criticalCss from '../styles/critical.css?inline';
import '../styles/theme.css';
import '../styles/tailwind.css';
import '../scripts/floating-cta.ts';



export interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  noindex?: boolean;
}

const {
  title = '24/7 Water Damage Restoration & Storm Roof Repair Lexington KY',
  description = 'Emergency water cleanup and professional storm damage roof restoration in Lexington. Fast 24/7 response, detailed damage documentation, and local KY expertise. Schedule your priority inspection.',
  canonical = SITE.url,
  noindex = false,
} = Astro.props as Props;

const robots = noindex ? 'noindex, nofollow' : 'index, follow';
const organizationSchema = generateOrganizationSchema(
  SITE.url,
  SITE.name,
  SITE.tel,
  SITE.email,
  SITE.addressLine
);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#00007e" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta name="robots" content={robots} />
    <link rel="icon" type="image/png" href="/assets/chat.icon.png" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITE.name} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={`${SITE.url}/_astro/roofer.DwIvtzSR_Z2vlGOz.webp`} />
    <meta property="og:image:width" content="1600" />
    <meta property="og:image:height" content="600" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${SITE.url}/_astro/roofer.DwIvtzSR_Z2vlGOz.webp`} />
    
    <!-- Critical above-the-fold CSS (inlined for faster rendering) -->
    <style set:html={criticalCss} />
    
    <!-- Defer non-critical stylesheets -->
    <link rel="stylesheet" href="/_astro/contact.Dn7m117j.css" media="print" onload="this.media='all'" />
    <noscript><link rel="stylesheet" href="/_astro/contact.Dn7m117j.css" /></noscript>
    
    <!-- Performance: Preload critical hero image to reduce LCP -->
    <link rel="preload" as="image" href="/assets/roofer.png" />
    
    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
  </head>
  <body class="min-h-screen bg-slate-50 text-slate-900">
    <a class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 rounded bg-white px-3 py-2 shadow" href="#main">
      Skip to content
    </a>
    <Header />
    <main id="main" class="min-h-[60vh]">
      <slot />
    </main>
    <Footer />
    <!-- Mobile Floating CTA Pill -->
    <div class="floating-cta" role="region" aria-label="Quick request inspection button">
      <a href="/request-inspection" class="floating-cta-button" aria-label="Request Inspection">
        Request Inspection
      </a>
    </div>
  </body>
</html>
