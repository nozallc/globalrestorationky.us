---
import Layout from '@layouts/Layout.astro';
import { SITE } from '../lib/site';
const title = `Water Damage Restoration (24/7) | ${SITE.name}`;
const description = 'Emergency water mitigation, drying, and restoration. Fast response across Central Kentucky.';
---

<Layout title={title} description={description} canonical={`${SITE.url}/water-damage`}>
  <section class="mx-auto max-w-6xl px-4 py-12">
    <h1 class="text-4xl font-black text-global-blue">Water Damage Restoration (24/7)</h1>
    <p class="mt-3 max-w-2xl text-base text-slate-700">
      When water enters your home, time matters. We focus on stopping the source, drying correctly, and restoring safely.
    </p>

    <div class="mt-8 grid gap-4 md:grid-cols-3">
      {[
        ['Emergency mitigation', 'Stop leaks, extract water, and protect affected areas.'],
        ['Dry-out & monitoring', 'Moisture tracking and drying to prevent mold and secondary damage.'],
        ['Rebuild & restore', 'Drywall, trim, flooring, and related repairs to pre-loss condition.'],
      ].map(([t, d]) => (
        <div class="rounded-2xl border border-slate-200 bg-white p-6">
          <div class="text-lg font-extrabold text-slate-900">{t}</div>
          <p class="mt-2 text-sm text-slate-600">{d}</p>
        </div>
      ))}
    </div>

    <div class="mt-10 rounded-2xl border border-slate-200 bg-slate-50 p-6">
      <div class="text-lg font-extrabold text-slate-900">For emergencies, call now</div>
      <p class="mt-2 text-sm text-slate-600">If you’re dealing with active leaking or flooding, call first.</p>
      <div class="mt-4 flex gap-3">
        <a class="rounded-lg bg-global-red px-4 py-2.5 text-sm font-semibold text-white hover:opacity-95" href={`tel:${SITE.tel}`}>Call {SITE.phone}</a>
        <a class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-900 hover:bg-white" href="/request-inspection">Request Inspection</a>
      </div>
    </div>
  </section>
</Layout>
